# Расписание ТКУИК

Современный сайт для отображения расписания занятий с двумя ролями: ученик и преподаватель.

## Возможности

- 📅 Просмотр расписания для учеников и преподавателей
- 🔄 Поддержка числителя/знаменателя недели
- ✏️ Редактирование расписания через админ-панель
- 📊 Просмотр всего расписания в виде таблицы
- 🎨 Современный дизайн с мягкими тенями и округлыми формами
- 📱 Адаптивная верстка

## Установка

1. Установите зависимости:

```bash
npm install
```

2. Инициализируйте базу данных:

```bash
npx prisma migrate dev --name init
npx prisma generate
```

3. Запустите сервер разработки:

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Использование

### Загрузка расписания

1. Перейдите на `/admin` для доступа к панели администратора
2. Загрузите Excel файлы для каждого курса (1-4)
3. Система автоматически распарсит данные и сохранит в БД

### Редактирование расписания

1. В админ-панели перейдите на вкладку "Просмотр и редактирование"
2. Выберите курс для фильтрации (или "Все курсы")
3. Нажмите "Изменить" для редактирования записи
4. Измените нужные поля и нажмите "Сохранить"
5. Для удаления записи нажмите "Удалить"

### Числитель и знаменатель

Система автоматически определяет текущую неделю (числитель/знаменатель) и показывает её на странице расписания.

В Excel файле можно указать тип недели:

- `(числ)` или `(ч)` - только числитель
- `(знам)` или `(з)` - только знаменатель
- Без маркера - обе недели

Пример: "Математика (числ)" будет показываться только на числителе.

## Формат Excel файла

Ожидаемые колонки:

- A: День недели (Понедельник, Вторник и т.д.)
- B: Время (например, 8:00-9:30)
- C: Предмет (можно добавить (числ) или (знам))
- D: Преподаватель
- E: Кабинет
- F: Группа (формат: 9ПО-319 или 9ПО-31к)

Пример строки:

```
Понедельник | 8:00-9:30 | Математика (числ) | Иванов И.И. | 201 | 9ПО-319
```

## Деплой на сервер

1. Установите Node.js на сервере (версия 18 или выше)
2. Склонируйте репозиторий
3. Установите зависимости: `npm install`
4. Инициализируйте БД: `npx prisma migrate deploy`
5. Соберите проект: `npm run build`
6. Запустите: `npm start`

### Использование PM2 (рекомендуется)

```bash
npm install -g pm2
pm2 start npm --name "schedule" -- start
pm2 save
pm2 startup
```

### Использование PostgreSQL вместо SQLite

Для продакшена рекомендуется использовать PostgreSQL:

1. Измените `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

2. Обновите `.env`:

```
DATABASE_URL="postgresql://user:password@localhost:5432/schedule"
```

3. Запустите миграцию:

```bash
npx prisma migrate deploy
```

## Технологии

- Next.js 14 (App Router)
- Prisma ORM
- SQLite (можно заменить на PostgreSQL/MySQL)
- TypeScript
- XLSX для парсинга Excel
- Современный CSS с градиентами и тенями

## Цветовая схема

- Основной цвет: #3e65a0 (синий ТКУИК)
- Фон: #f8f9fa (светло-серый)
- Акценты: градиенты и мягкие тени
